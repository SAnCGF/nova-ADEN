name: nova_aden
description: "APK nova-ADEN para gestión operativa de MIPYMES y TCP - Software Libre"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Base de datos local offline
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0
  # Generación de reportes PDF
  pdf: ^3.10.0
  printing: ^5.12.0
  # Utilidades
  intl: ^0.19.0
  provider: ^6.1.0
  # Seguridad para licencias
  flutter_secure_storage: ^9.0.0
  crypto: ^3.0.3
  # Info del dispositivo
  device_info_plus: ^10.1.0
  # Compartir archivos
  # Selector de archivos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
