name: nova_aden
description: "APK para gestión de compras, inventario, ventas y mermas en MIPYMES/TCP cubanos"
version: 1.0.0

environment:
  sdk: ^3.10.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.31.0
  sqlite3_flutter_libs: ^0.5.41
  path_provider: ^2.1.5
  path: ^1.9.0
  get_it: ^7.7.0
  provider: ^6.1.5
  pdf: ^3.10.4          # ✅ Aquí va
  printing: ^5.10.4     # ✅ Aquí va

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.11.0
  drift_dev: ^2.31.0

flutter:
  uses-material-design: true